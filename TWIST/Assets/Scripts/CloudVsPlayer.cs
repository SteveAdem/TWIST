using UnityEngine;
using System.Collections;

public class CloudVsPlayer : MonoBehaviour {

  /*  public GameObject TopContainer;
    private BGScroller BGScrollerScript;

	void Start () {
        BGScrollerScript = TopContainer.GetComponent<BGScroller>();
	}
	
	void Update () {
	    if(BGScrollerScript == null) { Debug.LogError("chora"); }
	}

    IEnumerator slowPlayer()
    {
        BGScrollerScript.scrollSpeed = -0.01f;
        Debug.Log("Colidiu");

        yield return new WaitForSeconds(2.0f);
        BGScrollerScript.scrollSpeed = -2f;
    }

    public void OnTriggerEnter(Collider col) {

        if(col.tag == "Player") {
            StartCoroutine(slowPlayer());
        }

    }*/
}
